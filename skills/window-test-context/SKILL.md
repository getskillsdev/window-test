---
name: window-test-context
description: V1 small desc - Context window version test plugin. This is version 1.0.0 with exactly 100 chars.
---

# Window Test Skill

This skill exists only to test how Claude loads plugin versions into the context window.

When asked "what version of window-test is in your context?", report the version from the description field above.
